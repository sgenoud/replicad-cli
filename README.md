# Replicad CLI

Repackages the replicad studio logic in a CLI. 

To install you need [deno on your machine](), and then run 


```
deno install --allow-write --allow-read https://deno.land/std/http/file_server.ts 
```
